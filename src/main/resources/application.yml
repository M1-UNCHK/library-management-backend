server:
  port: 9090

spring:
  application:
    name: library-management
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    generate-ddl: true

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/library-mngt
    username: unchk
    password: unchk@2025
    hikari:
      auto-commit: false

  banner:
    location: banner