spring:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    generate-ddl: true

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}?sslmode=require
    username: ${PGUSER}
    password: ${PGPASSWORD}
    hikari:
      auto-commit: false

  banner:
    location: banner

  mail:
    host: smtp.gmail.com
    port: 587
    username: rootsn221@gmail.com
    password: kzkv rusi wvdi bpyy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  thymeleaf:
    prefix: classpath:/mail/templates/
    suffix: .html
    encoding: UTF-8
    mode: HTML
    cache: false

security:
  cors:
    allowed-origins:
      - "*"
      - http://localhost:4200
    allowed-methods:
      - GET
      - POST
      - PUT
      - DELETE
      - PATCH
      - OPTIONS


application:
  url: http://library-management-backend-production-c80b.up.railway.app